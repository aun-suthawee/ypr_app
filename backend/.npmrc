# NPM Configuration for YPR Backend
# ลดการแสดง warnings และปรับปรุงประสิทธิภาพ

# ลดการแสดง deprecation warnings (แต่ยังคงแสดง errors)
loglevel=warn

# ใช้ registry ที่เร็วกว่า (ถ้าต้องการ)
# registry=https://registry.npmjs.org/

# ปิด audit warnings สำหรับ dev dependencies
audit-level=moderate

# เพิ่มความเร็วในการติดตั้ง
prefer-offline=true
cache-max=86400000

# ปิด funding messages
fund=false

# ลด progress indicators
progress=false
